# 상식 퀴즈 게임 — 클로드 코드 구현 3단계 프롬프트

> PRD(`PRD.md`) 기반으로 클로드 코드에서 순서대로 실행하세요.  
> 각 단계는 이전 단계 결과물 위에 쌓입니다.

---

## STEP 1 — MVP: 문제 데이터 + 게임 화면 + 즉시 피드백 + 점수 결과

```
상식 퀴즈 게임 MVP를 HTML/CSS/Vanilla JS 단일 파일(index.html)로 구현해줘.
아래 스펙을 정확히 따라야 해.

## 파일 구조
- index.html — 게임 전체를 담는 단일 파일
- data/questions.json — 40문제 데이터 (별도 파일)

## 문제 데이터 (data/questions.json)
아래 JSON 스키마로 카테고리 4개 × 10문제씩 총 40문제를 작성해.
{
  "id": "KH001",          // 카테고리 접두사 + 3자리 번호
  "category": "korean_history",  // korean_history | science | geography | general
  "question": "질문 텍스트",
  "options": ["보기1", "보기2", "보기3", "보기4"],
  "answer": 0,             // options 배열의 정답 인덱스 (0~3)
  "explanation": "1~2줄 해설"
}

카테고리별 주제 범위:
- korean_history: 고대~현대, 주요 사건/인물/문화재
- science: 물리/화학/생물/지구과학 기초 개념
- geography: 한국 및 세계 수도·지형·국가 정보
- general: 시사·문화·스포츠·예술 교양 지식

## 구현할 화면 (SPA 방식, 화면 전환은 CSS display로 제어)

### 1) 문제 화면
- 상단: 카테고리명, 진행 표시 (예: 3 / 40)
- 중앙: 질문 텍스트
- 하단: 4개 보기 버튼 (매 문제마다 보기 순서 랜덤 셔플)
- 우측 상단: 현재 점수

### 2) 피드백 화면 (문제 화면 위에 오버레이)
- 정답 선택 시: 해당 버튼 초록색 + "정답입니다! +10점" 메시지
- 오답 선택 시: 선택 버튼 빨간색 + 정답 버튼 초록색 + "오답입니다" 메시지
- 해설 텍스트 표시
- 다음 버튼을 누를 때까지 유지 (자동 넘어가기 없음)

### 3) 최종 결과 화면
- 총점 / 400점 표시
- 카테고리별 점수 4개 표시 (예: 한국사 80/100)
- 정답률(%) 표시
- 다시 하기 버튼 (게임 처음부터 재시작)

## 게임 로직
- 40문제를 카테고리 순서(한국사→과학→지리→일반상식)로 출제
- 각 카테고리 내 10문제는 랜덤 순서로 출제
- 정답 시 10점 추가, 오답 시 0점
- 한 세션에서 동일 문제 중복 출제 없음

## 디자인 요구사항
- 깔끔한 카드 레이아웃, 여백 충분히
- 보기 버튼: 호버 효과 포함, 선택 후 비활성화
- 색상: 정답 #22c55e(초록), 오답 #ef4444(빨강), 포인트 컬러는 자유롭게
- 기본 반응형 (모바일 375px, PC 768px 이상 대응)
- 외부 라이브러리 없이 순수 CSS만 사용
```

---

## STEP 2 — 순위 시스템: 이름 입력 + 로컬 리더보드

```
기존 index.html과 data/questions.json에 아래 기능을 추가해줘.
새 파일을 만들지 말고, 기존 파일을 수정해.

## 추가할 화면

### 1) 시작 화면 (기존 문제 화면 전에 추가)
- 게임 타이틀: "상식 퀴즈 게임"
- 사용자 이름 입력 필드 (최대 10자, 빈 값 제출 불가)
- 플레이 시작 버튼
- 리더보드 보기 버튼

### 2) 리더보드 화면
- 제목: "역대 순위"
- 상위 10명 순위표: 순위 / 이름 / 총점 / 플레이 일시
- 동점 시 플레이 일시 빠른 순으로 정렬
- 뒤로 가기 버튼

### 3) 최종 결과 화면에 추가
- 현재 플레이어 순위 표시 (예: "전체 7명 중 3위")
- 리더보드 보기 버튼 추가

## 로컬 스토리지 저장 스펙
key: "quiz_leaderboard"
value: JSON 배열, 각 항목:
{
  "username": "홍길동",
  "totalScore": 320,
  "categoryScores": {
    "korean_history": 80,
    "science": 90,
    "geography": 70,
    "general": 80
  },
  "playedAt": "2026-04-14T10:00:00.000Z"
}

## 저장 로직
- 게임 완료 시 자동으로 로컬 스토리지에 기록 추가
- 리더보드는 총점 내림차순 정렬 후 상위 10개만 유지
- 기존 기능(문제 출제, 피드백, 점수 계산)은 그대로 유지
```

---

## STEP 3 — UX 개선: 반응형 + 카테고리 선택 + 애니메이션

```
기존 index.html을 아래 기준으로 UX를 개선해줘.
기능을 제거하지 말고, 기존 동작 위에 추가/개선만 해.

## 1) 카테고리 선택 화면 추가 (시작 화면 다음에)
시작 화면에서 이름 입력 후 카테고리 선택 화면으로 이동.

선택지:
- "전체 플레이" — 40문제 (한국사 → 과학 → 지리 → 일반상식 순)
- "한국사" — 10문제만
- "과학" — 10문제만
- "지리" — 10문제만
- "일반상식" — 10문제만

각 카테고리 카드에 아이콘(이모지) 표시:
- 전체: 🏆, 한국사: 📜, 과학: 🔬, 지리: 🌍, 일반상식: 💡

## 2) 반응형 개선
- 모바일(~480px): 보기 버튼 세로 배치, 패딩 최소화
- 태블릿(481px~768px): 보기 버튼 2열 그리드
- PC(769px~): 보기 버튼 2열 그리드, 중앙 최대 너비 640px

## 3) 애니메이션 추가 (CSS transition/animation만 사용, JS 라이브러리 금지)
- 화면 전환: fadeIn 효과 (opacity 0→1, 0.3s)
- 보기 버튼 호버: scale(1.02) 변환
- 정답/오답 피드백: 정답은 pulse 효과, 오답은 shake 효과
- 점수 증가: 숫자가 튀어오르는 bounce 텍스트 효과

## 4) 진행 바(Progress Bar) 추가
- 문제 화면 상단에 가로 진행 바 표시
- 현재 문제 / 전체 문제 비율로 너비 변화 (CSS transition 0.3s)
- 색상: 카테고리별 구분색 사용 권장

## 5) 카테고리별 결과 중간 화면
- 전체 플레이 선택 시, 카테고리 10문제 완료마다 중간 결과 화면 표시
- 해당 카테고리 점수, 정답 수, 응원 메시지 표시
- "다음 카테고리" 버튼으로 계속 진행

## 유지 요구사항
- 기존 로컬 스토리지 리더보드 기능 완전 유지
- 단일 index.html 파일 구조 유지
- 외부 JS/CSS 라이브러리 추가 금지
```
